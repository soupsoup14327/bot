node_modules/
.env
*.log
bot.err
.DS_Store

# Runtime-artefacts produced by the bot itself — regenerated on each run.
# signals.json is a ring-buffer for autoplay context; metrics/** — diagnostics.
data/

